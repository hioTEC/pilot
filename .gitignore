# Secrets — never commit
.env
.secrets.age
.secrets.pub
keys/*.pem

# OS
.DS_Store

# Claude Code memory (managed per-machine via symlink, not committed here)
.claude/memory/

# gstack state
.gstack/
